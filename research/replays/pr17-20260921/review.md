# PR17 review

The independent read-only review found no remaining mathematical or repaired
implementation blockers. It reconstructed the pruned tree bottom-up at 274
arities, through power-of-two boundaries at 16,385, and checked 12,766 semantic
cases. Both repaired implementations matched its independently constructed DAG.
The anchor proof charges all unary names, discriminators and detectors, with
the robust pair in its original internal position. The reviewed manuscript
argument gives 2r-1 nodes and depth ceil(log2 r)+2; it is not Lean-formalized here.

The original tests evaluated a different fold from the profile recurrence.
The repair evaluates and measures the same materialized original-signature DAG.
The full mutation control now compares complete anchor outputs. Exact integer
certificates replace floating-point acceptance conditions; the only retained
floating-point expression displays a margin. Both original receipt byte strings
are unchanged. The CLI now enforces the same exact-byte expectation as check.sh.

The writer replayed both full checkers under ordinary and optimized Python and
ran both full CLI numeric-substitution rejection controls. Independent follow-up
also checked the byte-comparison path with receipt computation stubbed, without
counting that stub as mathematical replay. Raw report hashes are in qualification.
This is an internal agent review, not external peer review or a novelty finding.
