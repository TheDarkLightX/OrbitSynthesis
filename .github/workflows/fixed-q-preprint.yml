name: fixed-q-preprint

on:
  pull_request:
    paths:
      - '.github/workflows/fixed-q-preprint.yml'
      - 'experiments/audit_fixed_q_preprint.py'
      - 'experiments/test_preprint_subject.py'
      - 'experiments/audit_native_router_refinement.py'
      - 'notes/QUASIPRIMAL_CONSERVATIVE_TERM_*.md'
      - 'paper/FIXED_Q_TERM_COMPLEXITY_DRAFT.md'
      - 'paper/NATIVE_FIND_FIRST_ROUTER_*.md'
      - 'paper/preprint/**'
      - 'research/tournaments/2026-08-13-semantic-router-frontier/**'
      - 'research/tournaments/2026-08-13-program-vector-optimization/**'
      - 'research/replays/pr16-20260921/**'
      - 'research/replays/pr17-20260921/**'
      - 'research/RESEARCH_MAP_2026_08_13.md'
      - 'research/SOURCES.md'
      - 'lakefile.lean'
      - 'lean-toolchain'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  exact-evidence-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: leanprover/lean-action@v1
        with:
          test: false
          use-mathlib-cache: true
          build-args: '--wfail'
      - name: Replay frozen preprint evidence and Lean proofs
        run: |
          python3 -B experiments/test_preprint_subject.py
          python3 -B -O experiments/test_preprint_subject.py
          python3 experiments/audit_fixed_q_preprint.py
      - name: Replay native router refinement evidence and Lean proofs
        run: python3 experiments/audit_native_router_refinement.py
      - name: Replay exact order-pair vectors and Lean proofs
        run: bash research/tournaments/2026-08-13-semantic-router-frontier/lanes/order_pair_program_vector/check.sh
      - name: Replay constructed fast-anchor semantics and sharp-bound ledgers
        run: bash research/tournaments/2026-08-13-program-vector-optimization/lanes/fast_anchor_sharp_bounds/check.sh
