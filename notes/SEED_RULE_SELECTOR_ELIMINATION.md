# Seed selector elimination

**Status:** DERIVED exact logical normalization with finite calibration. Classical resolution machinery; OrbitSynthesis-specific content is the exact mapping from pointed seed rules to domain-only conflict certificates. Tau-independent. Not Lean-checked.

A full expansion is being kept deliberately theorem-oriented: no claim is made that selector variables, resolution, or clause learning are new.
